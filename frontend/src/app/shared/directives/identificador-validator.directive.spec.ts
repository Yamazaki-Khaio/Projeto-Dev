/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IdentificadorValidatorDirective } from './identificador-validator.directive';

describe('Directive: IdentificadorValidator', () => {
  it('should create an instance', () => {
    const directive = new IdentificadorValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
